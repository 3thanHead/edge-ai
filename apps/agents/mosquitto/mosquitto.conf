# Home-lab broker for the iot/# device topics. LAN-only trust model, same as
# the rest of the cluster: no auth, no TLS -- do not expose 1883 off-LAN.
listener 1883
allow_anonymous true
persistence false
